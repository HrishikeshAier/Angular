import { CompFailuerDirective } from './comp-failuer.directive';

describe('CompFailuerDirective', () => {
  it('should create an instance', () => {
    const directive = new CompFailuerDirective();
    expect(directive).toBeTruthy();
  });
});
